const SideNavbar = () => {
  return (
    <nav className="bg-primary h-screen">
      <div>Profile</div>

      <div>Todos List</div>

      <div>Logout</div>
    </nav>
  );
};

export default SideNavbar;
